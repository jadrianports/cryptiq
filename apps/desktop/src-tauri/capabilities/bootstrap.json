{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "cryptiq-bootstrap",
  "description": "Pre-path-choice capability: dialogs only. Active before the user has picked or created a vault path; the main fs scopes are in cryptiq-main.",
  "windows": ["main"],
  "platforms": ["windows", "macOS"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "dialog:allow-confirm"
  ]
}
