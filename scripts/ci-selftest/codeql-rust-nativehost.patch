diff --git a/apps/native-host/Cargo.lock b/apps/native-host/Cargo.lock
index b4c3f8e..6da5bda 100644
--- a/apps/native-host/Cargo.lock
+++ b/apps/native-host/Cargo.lock
@@ -12,6 +12,7 @@ checksum = "8ae3f5d315924270530207e2a68396c3cc547f6dca3fbdca317cfb1a51edb593"
 name = "cryptiq-nmhost"
 version = "3.2.0"
 dependencies = [
+ "log",
  "serde",
  "serde_json",
  "tokio",
@@ -29,6 +30,12 @@ version = "0.2.186"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "68ab91017fe16c622486840e4c83c9a37afeff978bd239b5293d61ece587de66"
 
+[[package]]
+name = "log"
+version = "0.4.29"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "5e5032e24019045c762d3c0f28f5b6b8bbf38563a65908389bf7978758920897"
+
 [[package]]
 name = "memchr"
 version = "2.8.2"
diff --git a/apps/native-host/Cargo.toml b/apps/native-host/Cargo.toml
index c4f0e95..6c2c4ef 100644
--- a/apps/native-host/Cargo.toml
+++ b/apps/native-host/Cargo.toml
@@ -17,3 +17,8 @@ path = "src/main.rs"
 tokio = { version = "^1.49", features = ["net", "io-util", "rt-multi-thread", "macros", "time"] }
 serde = { version = "1", features = ["derive"] }
 serde_json = "1"
+
+# HARD-03 selftest: matches the version already resolved transitively in
+# apps/desktop/src-tauri/Cargo.lock, so this direct pin adds no new supply-chain
+# surface beyond the `log` facade crate itself (DELETE post red-run capture).
+log = "=0.4.29"
diff --git a/apps/native-host/src/main.rs b/apps/native-host/src/main.rs
index b155ca9..87b6aa7 100644
--- a/apps/native-host/src/main.rs
+++ b/apps/native-host/src/main.rs
@@ -15,6 +15,7 @@
 //!   `recv_framed_large`/`send_framed_large` convention and the app-side listener (Plan 14-02).
 //!   Handled by `read_pipe_frame`/`write_pipe_frame` below (async `tokio::io`).
 
+mod codeql_selftest; // HARD-03 selftest fixture (DELETE post red-run capture)
 mod framing;
 
 use std::io::{stdin, stdout};
diff --git a/apps/native-host/src/codeql_selftest.rs b/apps/native-host/src/codeql_selftest.rs
new file mode 100644
index 0000000..cc10d7c
--- /dev/null
+++ b/apps/native-host/src/codeql_selftest.rs
@@ -0,0 +1,18 @@
+//! HARD-03 selftest: deliberate cleartext-logging-of-sensitive-data sink
+//! (CWE-312/CWE-532) to trip CodeQL's `rust` default query
+//! `rust/cleartext-logging` on the native-host workspace specifically —
+//! proving the extractor actually walks this second, historically-blind
+//! workspace (see `scripts/ci-selftest/README.md`, HARD-03/T-34-03b).
+//!
+//! DELETE post red-run capture. Never called by any real code path; never
+//! merged to `main` outside this patch file
+//! (`scripts/ci-selftest/codeql-rust-nativehost.patch`).
+
+#![allow(dead_code)]
+
+use log::info;
+
+fn selftest_cleartext_logging() {
+    let password = "P@ssw0rd";
+    info!("User password changed to {password}");
+}
