import { describe, it, expect } from 'vitest';

// Wave 0 stub — real DC-6 tiered-padding tests land in Wave 3.
describe('crypto/padding', () => {
  it('stub — implemented in wave 3', () => {
    expect(true).toBe(true);
  });
});
