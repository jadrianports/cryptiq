import { describe, it, expect } from 'vitest';

// Wave 0 stub — real Crockford Base32 recovery-key tests land in Wave 3.
describe('crypto/recovery', () => {
  it('stub — implemented in wave 3', () => {
    expect(true).toBe(true);
  });
});
