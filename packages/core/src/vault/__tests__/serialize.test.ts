import { describe, it, expect } from 'vitest';

// Wave 0 stub — real parseOuter()/serializeOuter()/encryptInner()/decryptInner()
// tests land in Wave 3.
describe('vault/serialize', () => {
  it('stub — implemented in wave 3', () => {
    expect(true).toBe(true);
  });
});
