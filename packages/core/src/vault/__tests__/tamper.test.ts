import { describe, it, expect } from 'vitest';

// Wave 0 stub — DC-11 9-region single-byte tamper tests land in Wave 3.
describe('vault/tamper', () => {
  it('stub — implemented in wave 3', () => {
    expect(true).toBe(true);
  });
});
