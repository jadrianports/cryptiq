import { describe, it, expect } from 'vitest';

// Wave 0 stub — synthetic v0→v1 mock-migration + cold-decrypt-verify tests land in
// Wave 4 (TEST-03).
describe('vault/migration', () => {
  it('stub — implemented in wave 4', () => {
    expect(true).toBe(true);
  });
});
