import { describe, it, expect } from 'vitest';

// Wave 0 stub — full create→save→load→unlock round-trip + KAT tests land in Wave 3.
describe('vault/round-trip', () => {
  it('stub — implemented in wave 3', () => {
    expect(true).toBe(true);
  });
});
