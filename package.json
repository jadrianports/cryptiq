{
  "name": "cryptiq",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.5.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "pnpm -r build && pnpm --filter ./apps/desktop tauri build",
    "dev": "pnpm --filter ./apps/desktop tauri dev",
    "lint": "eslint . && node scripts/lint/lint-workflow-sha-pins.mjs && node scripts/lint/lint-capability-globs.mjs && node scripts/lint/lint-capability-platforms.mjs && node scripts/lint/lint-csp.mjs && node scripts/lint/lint-supply-chain.mjs",
    "lint:custom": "node scripts/lint/lint-workflow-sha-pins.mjs && node scripts/lint/lint-capability-globs.mjs && node scripts/lint/lint-capability-platforms.mjs && node scripts/lint/lint-csp.mjs && node scripts/lint/lint-supply-chain.mjs",
    "test": "vitest run",
    "typecheck": "pnpm -r --workspace-concurrency=1 run typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript-eslint": "^8.59.4",
    "eslint-plugin-svelte": "^3.17.1",
    "svelte-eslint-parser": "^1.6.1",
    "prettier": "^3.3.0",
    "vitest": "^3.2.4",
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@tauri-apps/cli"
    ]
  }
}
